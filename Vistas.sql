/*--------------------------------------------------VISTAS------------------------------------------------------------------------------*/
CREATE VIEW CategoryView AS
SELECT id, name,description
FROM Category;

CREATE VIEW BillStateView AS
SELECT id, name
FROM Bill_State;

